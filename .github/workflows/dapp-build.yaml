name: DAPP build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  dapp_build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js (Smart contract)
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: yarn
          cache-dependency-path: smart-contract/yarn.lock

      - name: Yarn install (Smart contract)
        run: cd ./smart-contract && yarn install
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Compile smart contract
        run: cd ./smart-contract && yarn compile

      - name: Setup Node.js (DAPP)
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: yarn
          cache-dependency-path: minting-dapp/yarn.lock

      - name: Yarn install (DAPP)
        run: cd ./minting-dapp && yarn install
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build DAPP
        run: cd ./minting-dapp && yarn build
```

**What changed:**
- `actions/checkout@v2` → `actions/checkout@v4`
- `actions/setup-node@v2` → `actions/setup-node@v4`
- Added `node-version: '20'` to both setup-node steps

---

**Step 1 — Open the workflow file in VS Code**

Make sure you're in the `erc721-nft-test` folder, then open:
```
.github/workflows/dapp-build.yaml
